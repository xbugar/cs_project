<UserControl x:Class="ExpenseManager.Components.Input"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="50" d:DesignWidth="300" Background="Transparent"
             x:Name="InputControl">
    
    <Border HorizontalAlignment="Stretch" Margin="5">
        <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" Background="Transparent">
            <Grid HorizontalAlignment="Stretch" Margin="5">
                <TextBlock Foreground="#99999999" Background="Transparent"
                           Text="{Binding Path=Text, ElementName=InputControl}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Path=TextBinding, ElementName=InputControl}" Value="">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Path=TextBinding, ElementName=InputControl}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                            <Setter Property="Visibility" Value="Hidden" />
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                <TextBox Background="Transparent" BorderBrush="Transparent" Foreground="AliceBlue"
                         Text="{Binding Path=TextBinding, ElementName=InputControl, UpdateSourceTrigger=PropertyChanged}" />
            </Grid>
            <Rectangle Height="2" Fill="AliceBlue" HorizontalAlignment="Stretch">
                <Rectangle.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="5" Opacity="1" />
                </Rectangle.Effect>
            </Rectangle>
        </StackPanel>
    </Border>
</UserControl>
